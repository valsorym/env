TODO List

ID:     0001
TITLE:  Processing variables as parts of the value.
STATUS: DONE
PROBLEM:
    Example: file.env
            export KEY_0="value_0"
            export KEY_1="${KEY_0}01"
    Result must be as:
            KEY_0=value_0
            KEY_1=value_001
    P.s. The KEY_1 not as `${KEY_0}01` but as `value_001`.
-------------------------------------------------------------------------------
ID:     0002
TITLE:  Add synonyms for functions from the os package
        for working with the environment.
STATUS: DONE
PROBLEM:
    Add synonyms:
    os.Getenv   => env.Get
    os.Setenv   => env.Set
    os.Unsetenv => env.Unset
    os.Clearenv => env.Clear
    os.Environ  => env.Environ
-------------------------------------------------------------------------------
ID:     0003
TITLE:  Add simple load- functions.
STATUS: DONE
PROBLEM:
    For example:
    Load       load new keys without replacing existing ones and make expand;
    LoadSafe   load new keys without replacing existing ones;
    Update     load new keys with replacing existing ones and make expand;
    UpdateSafe load new keys with replacing existing ones.
    etc.
-------------------------------------------------------------------------------
ID:     0004
TITLE:  Add Exist function.
STATUS: DONE
PROBLEM:
    For example:
    if env.Exist("KEY_0") {
        // to do something ...
    } else {
        env.Set("KEY_0", "DEFAULT_VALUE")
    }
-------------------------------------------------------------------------------
ID:     0005
TITLE:  Add Decode function.
STATUS: DONE
NOTE: Added Unmarshal method.
PROBLEM:
    For example:
    env-file:
    HOST="localhost"
    PORT=8080
    PATH_LIST=/first/path:/second/path

    go-code:
    type Config struct {
        Host     string   `env:"HOST"`
        Port     uint     `env:"PORT"`
        PathList []string `env:"PATH_LIST,:"`
    }

    ...
    var c = &Config{}
    env.Decode(c)

    ...
    c.Host     // localhost
    c.Port     // 8080
    c.PathList // []string{"/first/path", "/second/path"}
-------------------------------------------------------------------------------
ID:     0006
TITLE:  Add Marshal method.
STATUS: PROCESS
PROBLEM:
    Put structure fields into environment - the opposite of Unmarshal func.
-------------------------------------------------------------------------------
ID:     0007
TITLE:  Doc.
STATUS: ...
PROBLEM:
    Add usage examples...
